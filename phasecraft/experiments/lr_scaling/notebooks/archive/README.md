# LR scaling notebook archive

## Contents

| File | Notes |
|------|--------|
| `LR_QAOA_benchmark_efficient_legacy_root_copy.ipynb` | Former `phasecraft/LR_QAOA_benchmark_efficient.ipynb` (package root). Archived 2026-06-02. |

**Canonical notebook (use for new runs):**  
`phasecraft/experiments/lr_scaling/notebooks/LR_QAOA_benchmark.ipynb`  
(formerly `LR_QAOA_benchmark_efficient.ipynb`)

## Deferred hard-delete candidates

After you confirm git history and the canonical notebook are sufficient, these archived files *may* be deleted in a future cleanup (not required):

| Path | Safe to delete later? |
|------|----------------------|
| `LR_QAOA_benchmark_efficient_legacy_root_copy.ipynb` | **Maybe** — only if you no longer need old cell outputs from the root copy (~116 KB variant) |

Do **not** delete the canonical notebook, Python training scripts, or `bm24_runs` artifacts without a separate audit.
